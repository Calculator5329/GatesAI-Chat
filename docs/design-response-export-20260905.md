# A23 response download — 2026-09-05

Root adopted one Download response (.md) action in the existing assistant action row. The file begins with the exact messageText snapshot and ends with a separate provenance section containing thread/message identity, message creation time, export time, and the displayed model label. Text is not reconstructed from rendered HTML. Source thread identity is passed with the message through the windowed renderer; it is never looked up from current selection at click time.

The action is absent for non-assistant messages and disabled for empty text, streaming, or unavailable source identity. Each deliberate click may initiate another browser download. Use existing inline failure feedback; successful initiation makes no Saved or durable completion claim. No Bridge request, workspace path, store, registration, or schema change. This delivers the download slice only, not the full workspace deliverable roadmap item.

Interaction lens: streamed message parts produce messageText; the message renderer receives source identity from its parent; Blob download consumes an immutable string created during the click. Adversarial lens: switching threads cannot change provenance; streaming/empty/nonassistant inputs cannot export; untrusted provenance strings are JSON encoded and cannot close the metadata fence. Running lens: existing component suite plus real browser download receipts exercise actual clicks and downloaded bytes; new cases are diagnostic probes, with full pre-existing CI/journeys and independent root review as acceptance gates.

Desktop investigation: existing dataExport.ts and HtmlArtifactPreview.ts use Blob/anchor downloads; the Rust shell has no custom download override. Official Wry documentation describes a default handler allowing downloads (https://docs.rs/wry/latest/wry/struct.WebViewBuilder.html#method.with_download_started_handler). Browser acceptance does not verify native WebView download completion. This slice neither adds native filesystem access nor claims a verified native save.

Corrections retained: session end releases the actual holder's leases; do not issue redundant lease release. The A33 collision correction preserves existing ordered writes for normalized filename collisions; this download slice delegates filename collision handling to the user's download agent rather than touching existing workspace files.

Implementation follows UI → existing ChatStore facade → responseExport service; the initial direct service import was rejected by lint and corrected without changing the lint boundary. The installed shared node_modules compiler was stale versus the delivered vendor package. Its unsupported `--check` flag caused regeneration rather than validation; that output was archived in the campaign cache. Regeneration with the exact extracted vendored compiler restored journey output byte-identical to HEAD. Only the registry changes for the new action and source locations. Inspect command syntax before using flags; lease claims use separate path arguments and their receipt must succeed before dependent edits.

Real Playwright download acceptance passed in desktop-mode Chromium and Web Lite, with no Bridge available: hover the existing message row to reveal actions, download while offline after reload, read exact UTF-8 bytes, match source thread/message identity, and repeat the deliberate click. Native WebView completion is not measured. An initial browser diagnostic attempted to click a hidden hover-only action; the corrected interaction uses hover, never forced clicks or CSS changes.

Verification receipt: full npm run ci passed 1361 tests plus typecheck/lint; full npm run test:e2e -- --workers=2 passed 146 tests with zero retries on isolated ports15523/15524. The focused download cases passed both browser runtimes. Existing vendor compiler scan check passes and compiled journeys remain byte-identical to base. Logs: campaign cache response-export-ci-final.log, response-export-browser-final.log, response-export-e2e.log. Independent reviewer found no blocker. Actual thread-switch/windowing and modelLabel assertions remain source-reviewed rather than added browser measurements.

Named, not built: workspace save still lacks atomic no-overwrite semantics at Bridge fs.write. Native WebView download completion remains unmeasured; no native-save claim. Browser initiation does not confirm owner-chosen destination or durable storage. The full first-class deliverables roadmap item stays open.
