// Shared constants for the database plugin layer. The host version default is a
// fallback for the compatibility check; the wiring lane passes the real app
// version through DatabasePluginManagerOptions.
export const DEFAULT_HOST_VERSION = '4.6.1'
